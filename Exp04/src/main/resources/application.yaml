server:
  port: 8080

spring:
  application:
    name: student-grade-system
  datasource:
    url: jdbc:mysql://localhost:3306/stu_grade_sys?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.cqu.exp04.entity

logging:
  level:
    com.cqu.exp04.mapper: debug
    com.cqu.exp04: info
    org.springframework.security: warn  # 降低 Security 日志级别

# JWT Configuration
jwt:
  secret: KSH6vF7U1eLW/xQi831jARHg6/Z+0bOF44LwOQg39UQ=
  expiration: 86400000  # 24 hours in milliseconds

# LangChain4j Configuration
langchain4j:
  dashscope:
    chat-model:
      api-key: ${DASHSCOPE_API_KEY:sk-664256e37ba2498db97965cfa62178aa}
      model-name: qwen-max
      temperature: 0.7
      top-p: 0.8
      enable-search: true

# Optional: auto-create SUPER_ADMIN on startup.
# Leave empty to disable.
app:
  super-admin:
    username: nissoncx
    password: 12345678

